 Создание пользователя и БД в PostgreSQL:
        # sudo -u postgres createuser username
        # sudo -u postgres psql -c "ALTER USER username PASSWORD 'password';"
        # sudo -u postgres createdb -O username namedatabase
